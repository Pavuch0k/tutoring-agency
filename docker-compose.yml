version: '3.8'

services:
  tutoring-app:
    build: .
    ports:
      - "3001:3000"  # Порт 3001 на хосте -> 3000 в контейнере
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    container_name: tutoring-agency